Implementa la FASE 2 de program-prompt: los comandos "chat" y "edit".

REGLAS DURAS (incumplirlas rompe el trabajo de otros):
- Trabaja SOLO dentro de C:/Users/ivans/.claude/tools/chat-queue
- ADITIVO: NO renombres archivos ni comandos existentes. NO borres el hook /programar,
  ni programar.mjs, ni importProgramados. NO cambies la firma de comandos que ya existen.
- NO toques datos del usuario: data/, out/, projects.json, programados.jsonl
- Reutiliza lib/ (store.mjs, time.mjs, ui.mjs, runner.mjs). No dupliques logica.
- Escribe tests en test/ para TODO lo que anadas.
- OBLIGATORIO antes de terminar: "node --check" de todos los .mjs Y "node --test test/*.test.mjs"
  con TODOS los tests en verde. Si alguno falla, arreglalo. NUNCA termines con tests rojos.
- Haz git commit al terminar, con mensaje claro.
- Antes de tocar nada: lee README.md y el plan en
  C:/Users/ivans/.claude/plans/wise-wibbling-dewdrop.md (secciones Fase 2/3/4).

QUE HACER:
1) Nuevo lib/chat.mjs + comando "program-prompt chat <id|target|session-id>":
   - Resuelve target -> session-id via data/sessions.json, o acepta un session-id o un id de job.
   - Localiza el transcript en ~/.claude/projects/<carpeta-codificada>/<sid>.jsonl
     La codificacion sustituye : \ / . por - (ej: C:\Users\x\.claude\tools -> C--Users-x--claude-tools)
   - Formato del transcript: JSONL con type: user|assistant, message.content
     (string en user; array de bloques en assistant), timestamp, cwd.
   - Renderiza con el estilo de lib/ui.mjs: cabecera (target, sesion, carpeta, nº turnos,
     fechas) + la conversacion (texto y tool_use resumidos, tipo "Read(app/main.py)").
   - Flags: --last N (por defecto 20), --full, --raw.
   - Si no existe el transcript, error claro (no crash).
2) Comando "program-prompt edit <id>":
   - Llama a importProgramados() primero.
   - Solo jobs pending (si esta done/running: rechaza con mensaje claro).
   - Flags: --prompt, --at, --target, --dir, --perm, --adapter. Reusa parseWhen y resolveDir.
   - Muestra el job resultante tras editar.
3) Actualiza HELP y README.md con ambos comandos.
