Implementa la FASE 3 de program-prompt: la GUI guiada.

REGLAS DURAS (incumplirlas rompe el trabajo de otros):
- Trabaja SOLO dentro de C:/Users/ivans/.claude/tools/chat-queue
- ADITIVO: NO renombres archivos ni comandos existentes. NO borres el hook /programar,
  ni programar.mjs, ni importProgramados. NO cambies la firma de comandos que ya existen.
- NO toques datos del usuario: data/, out/, projects.json, programados.jsonl
- Reutiliza lib/ (store.mjs, time.mjs, ui.mjs, runner.mjs). No dupliques logica.
- Escribe tests en test/ para TODO lo que anadas.
- OBLIGATORIO antes de terminar: "node --check" de todos los .mjs Y "node --test test/*.test.mjs"
  con TODOS los tests en verde. Si alguno falla, arreglalo. NUNCA termines con tests rojos.
- Haz git commit al terminar, con mensaje claro.
- Antes de tocar nada: lee README.md y el plan en
  C:/Users/ivans/.claude/plans/wise-wibbling-dewdrop.md (secciones Fase 2/3/4).

QUE HACER:
- "program-prompt" SIN argumentos abre una GUI a pantalla completa (los subcomandos siguen igual).
- Nuevo lib/tui.mjs, con el estilo de Claude Code (usa lib/ui.mjs: bordes redondeados, acento
  naranja, texto tenue, barra de atajos abajo).
- Vistas: Cola · Chats/Sesiones · Proyectos · Ayuda.
- Teclas: flechas mover, Enter detalle, a=anadir (asistente paso a paso: prompt -> cuando ->
  target -> carpeta -> permisos), e=editar, d=borrar, r=lanzar (entra al reloj del runner),
  o=ver salida, c=ver chat, q=salir, ?=ayuda.
- Usa process.stdin en raw mode; restaura SIEMPRE la terminal al salir (mira installCleanup en ui.mjs).
- CRITICO: si no hay TTY (process.stdout.isTTY falso), NO abras la GUI: imprime la ayuda.
  El uso desatendido no se puede romper.
- Reutiliza los comandos que ya existen (no dupliques la logica de add/edit/rm/run).
